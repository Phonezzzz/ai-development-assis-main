# AI Development Assistant - Environment Variables

# OpenRouter API (для LLM запросов)
VITE_OPENROUTER_API_KEY=your_openrouter_api_key_here
VITE_OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Local LLM (Ollama/LM Studio)
# Пример: http://localhost:11434 (Ollama) или http://localhost:1234 (LM Studio)
VITE_LOCAL_LLM_URL=http://localhost:11964

# OpenAI API (для embeddings)
VITE_OPENAI_API_KEY=your_openai_api_key_here
VITE_OPENAI_BASE_URL=https://api.openai.com/v1

# ElevenLabs API (для TTS)
VITE_ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
VITE_ELEVENLABS_BASE_URL=https://api.elevenlabs.io/v1

# Qdrant Vector Database
VITE_QDRANT_URL=http://localhost:6333
VITE_QDRANT_API_KEY=your_qdrant_api_key_here

# --- Speech-To-Text (Whisper via local Vite proxy) ---
# Серверный ключ OpenAI для STT-прокси (используется на стороне dev/preview-сервера Vite)
OPENAI_API_KEY=your_openai_api_key_here

# Лимиты и таймауты STT (опционально)
# Максимальный размер загружаемого аудио (в байтах). По умолчанию 15728640 (15MB)
STT_MAX_BYTES=15728640
# Таймаут запроса к OpenAI (в мс). По умолчанию 60000 (60s)
STT_TIMEOUT_MS=60000
# Разрешённый Origin для CORS (для локальной разработки можно оставить "*")
CORS_ORIGIN=*

# Инструкции:
# 1. Скопируйте этот файл как .env
# 2. Замените значения на ваши реальные API ключи
# 3. Не комитьте .env файл в git
