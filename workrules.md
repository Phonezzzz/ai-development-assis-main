
 🚫 ПРАВИЛА РАЗРАБОТКИ

**ЭТО ПРИВАТНЫЙ ПЕТ-ПРОЕКТ, НЕ ПРОДАКШН**

## 🚨 КРИТИЧНО: ПРОВЕРКА TYPESCRIPT

### ПОСЛЕ изменений файла:

1. Запусти npm run build

2. Смотри на ошибки:

### ЕСЛИ ошибки в ИЗМЕНЁННОМ файле:
✅ Исправь их БЕЗ костылей (any, as unknown, etc)
✅ Это твоя ответственность

### ЕСЛИ ошибки в ДРУГИХ файлах (не твоих):
❌ НЕ ТРОГАЙ ИХ!
❌ НЕ ИСПРАВЛЯЙ!
⚠️  Просто сообщи:
```
✅ Мои изменения сделаны
⚠️  npm run build показывает ошибки в других файлах:

Эти ошибки были ДО моих изменений.
Исправить их отдельной задачей?
```

### НИКОГДА НЕ ДЕЛАЙ:

❌ `as any`
❌ `as unknown as Type`  
❌ `// @ts-ignore`
❌ Любые костыли чтобы "сборка прошла"

**Лучше СООБЩИТЬ об ошибке, чем СКРЫТЬ её!**

## ⚡ ГЛАВНЫЙ ПРИНЦИП: FAIL-FAST

**Если что-то может упасть - пусть падает СРАЗУ и ГРОМКО**

❌ **НЕ скрывай ошибки**  
❌ **НЕ делай "безопасный" код**  
❌ **НЕ используй фолбэки "на всякий случай"**  

✅ **Я ХОЧУ видеть ошибки немедленно**  
✅ **Ошибка = проблема которую надо ИСПРАВИТЬ, а не СКРЫТЬ**

---

## 🚨 АБСОЛЮТНЫЕ ЗАПРЕТЫ

### 1. ЗАПРЕЩЕНО: Silent Fallback

```typescript
// ❌ ЗАПРЕЩЕНО:
try {
  await someOperation();
} catch (error) {
  console.error('Error:', error);
  // ← ничего не происходит, ошибка скрыта
}

// ❌ ЗАПРЕЩЕНО:
try {
  const result = await apiCall();
  return result;
} catch {
  return null; // ← скрываем проблему
}

// ✅ ПРАВИЛЬНО - пусть упадёт:
const result = await apiCall(); // если упадёт - покажет ошибку

// ✅ ПРАВИЛЬНО - если РЕАЛЬНО нужен try-catch:
try {
  await apiCall();
} catch (error) {
  // 1. Логируй
  console.error('API call failed:', error);
  // 2. Покажи пользователю
  toast.error('Не удалось загрузить данные');
  // 3. БРОСЬ ОШИБКУ ДАЛЬШЕ
  throw error;
}
```

### 2. ЗАПРЕЩЕНО: Optional Chaining вместо исправления типов

```typescript
// ❌ ЗАПРЕЩЕНО:
const name = user?.profile?.name // ← почему user может быть undefined?

// ✅ ПРАВИЛЬНО:
// 1. Исправь типы чтобы user был гарантированно
interface Props {
  user: User; // ← НЕ User | undefined
}

// 2. Или проверь явно и упади:
if (!user) {
  throw new Error('User is required');
}
const name = user.profile.name;
```

### 3. ЗАПРЕЩЕНО: Fallback значения

```typescript
// ❌ ЗАПРЕЩЕНО:
const url = config.url || 'http://default.com' // ← скрывает проблему

// ✅ ПРАВИЛЬНО:
if (!config.url) {
  throw new Error('URL is required in config');
}
const url = config.url;
```

### 4. ЗАПРЕЩЕНО: any типы

```typescript
// ❌ ЗАПРЕЩЕНО:
function getData(): any { ... }
const user: any = getUser();

// ✅ ПРАВИЛЬНО:
// Используй СУЩЕСТВУЮЩИЕ типы из src/lib/types.ts:
import { Message, PendingPlan, TodoList } from '@/lib/types';

const messages: Message[] = getMessages();
const plan: PendingPlan | null = getPlan();
```

---

## 📋 ОБЯЗАТЕЛЬНАЯ ПРОЦЕДУРА ПЕРЕД РАБОТОЙ

### ШАГ 1: Прочитай задачу ПОЛНОСТЬЮ

1. Прочитай ВСЁ сообщение от начала до конца
2. Перечисли что просят сделать (чек-лист)
3. Если что-то НЕПОНЯТНО - СПРОСИ
4. ПОТОМ начинай работу

**Если ты сделал "стандартное решение" - ты НЕ ПРОЧИТАЛ задачу!**

### ШАГ 2: Покажи ЧТО будешь менять

ПЕРЕД началом работы:
1. Покажи список файлов которые изменишь
2. Объясни ЧТО сделаешь
3. Объясни ЗАЧЕМ это нужно
4. Дождись подтверждения "ОК, делай"

### ШАГ 3: Ищи ПРИЧИНУ проблемы

```
❌ НЕ ДЕЛАЙ ТАК:
Видишь ошибку → добавляешь try-catch → проблема "решена"

✅ ДЕЛАЙ ТАК:
Видишь ошибку → ищешь ПОЧЕМУ она возникла → исправляешь ПРИЧИНУ
```

---

## 🎯 РАБОТА С ТИПАМИ

### Используй СУЩЕСТВУЮЩИЕ типы

**В проекте УЖЕ ЕСТЬ типы в src/lib/types.ts:**

- Message - сообщения чата
- PendingPlan - планы задач  
- TodoList, TodoItem - списки задач
- WorkRule - рабочие правила
- ModelOption - модели
- ProjectFile - файлы проекта

**ВСЕГДА проверяй этот файл ПЕРЕД созданием новых типов!**

---

## 🔍 ПОСЛЕ РАБОТЫ

### Обязательная проверка:

```bash
# 1. Запусти проект:
npm run dev

# 2. Проверь TypeScript:
npm run build
```

---

## 💡 ПОМНИ

1. Это **ПРИВАТНЫЙ** проект - простота важнее "правильности"
2. **FAIL-FAST** - ошибки должны быть видны сразу
3. **НЕТ ФОЛБЭКАМ** - не скрывай проблемы
4. **ИСПОЛЬЗУЙ ТИПЫ** - они уже есть в проекте
5. **СПРАШИВАЙ** если непонятно - не додумывай

